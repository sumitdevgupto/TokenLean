# Direct test dependencies — HUMAN-MAINTAINED floors.
# requirements-test.txt is COMPILED from this file by scripts/compile-requirements.sh
# (constrained to src/proxy's pinned resolve so shared deps pin identically).
# Edit HERE, then regenerate; never edit requirements-test.txt by hand.
pytest>=8.0
pytest-asyncio>=1.4.0
pytest-mock>=3.15.1
pytest-cov>=7.1.0
httpx>=0.27
pyyaml>=6.0.3
prometheus_client>=0.20
